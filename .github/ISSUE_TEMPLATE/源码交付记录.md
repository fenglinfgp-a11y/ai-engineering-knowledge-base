# 源码交付记录

来源类型：实战总结

## 问题背景

- 项目类型：`<backend/frontend/fullstack>`
- 交付目标：`<handover/review/deployment>`
- 接收方：`<internal/external-placeholder>`

## 环境信息

- 后端技术：`<backend-stack>`
- 前端技术：`<frontend-stack>`
- 数据库：`<database-type>`

## 现象描述

描述交付包缺少源码、数据库结构、README、环境变量示例或混入敏感文件的情况。

## 已执行操作

```bash
find . -maxdepth 3 -type f | sort
```

用途：查看交付包结构。

```bash
find . -type f \( -name ".env" -o -name "*.pem" -o -name "*.key" -o -name "*.sql" \)
```

用途：查找环境变量、私钥、密钥和 SQL 文件，结果必须人工脱敏。

## 关键日志

```text
<脱敏后的缺失项或风险项记录>
```

## 判断结论

- backend 是否完整：
- frontend 是否完整：
- database 是否完整：
- 是否可继续开发：
- 是否可部署：

## 解决方案

列出需要补齐或清理的文件，不保存真实客户数据。

## 验证结果

- [ ] 后端可启动
- [ ] 前端可构建
- [ ] 空库可初始化
- [ ] 敏感信息已清理

## 是否已脱敏

- [ ] 是，已脱敏
- [ ] 否，暂不写入知识库

## 后续沉淀位置

建议路径：`cases/delivery/<case-name>.md`
