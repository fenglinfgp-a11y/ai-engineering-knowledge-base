# 常用命令索引

来源类型：实战总结

## 问题场景

需要快速查找常用排查命令及其用途。

## 判断方法

- Android 用 `adb`、`gradlew`。
- 后端服务用 `curl`、`systemctl`、`journalctl`。
- 前端用 `npm`、`node`。
- 数据库用 `mysql`、`mysqldump`。
- 服务器用 `ss`、`nginx`、`ufw`、`docker`。
- 分主题命令库优先看 `commands/` 目录。
- 提交和同步流程看 `workflows/GitHub同步流程.md`。
- 命令风险判断看 `security/命令风险分级.md`。

## 操作步骤

```bash
adb devices
```

用途：确认 Android 测试设备连接状态。

```bash
adb logcat -v time
```

用途：查看 Android 设备实时日志。

```bash
./gradlew assembleDebug --stacktrace
```

用途：构建 Android Debug 包并输出详细异常。

```bash
curl -i http://127.0.0.1:<port>/health
```

用途：查看本机服务接口响应状态码和响应头。

```bash
systemctl status <service-name>
```

用途：查看系统服务状态。

```bash
journalctl -u <service-name> -n 200 --no-pager
```

用途：查看 systemd 服务最近 200 行日志。

```bash
nginx -t
```

用途：检查 Nginx 配置语法。

```bash
ss -lntp
```

用途：查看 TCP 监听端口和对应进程。

```bash
docker ps -a
```

用途：查看所有 Docker 容器状态。

```bash
mysqldump -h <host> -u <user> -p --no-data <database_name> > schema.sql
```

用途：只导出 MySQL 表结构，适合源码交付。

## 命令专题入口

| 类型 | 文档 |
| --- | --- |
| Linux 系统状态、磁盘、内存、日志 | `commands/linux常用命令.md` |
| Nginx 配置检查、reload、日志 | `commands/nginx常用命令.md` |
| MySQL 连接、结构、备份、EXPLAIN | `commands/mysql常用命令.md` |
| Docker 容器、日志、Compose | `commands/docker常用命令.md` |
| Java / Spring Boot 服务排查 | `commands/java服务常用命令.md` |
| Git 状态、diff、提交记录 | `commands/git常用命令.md` |
| GitHub Desktop 同步流程 | `workflows/GitHub同步流程.md` |
| Codex 修改前检查命令 | `workflows/Codex协作流程.md` |
| 命令风险分级和危险操作判断 | `security/命令风险分级.md` |

## 第三阶段案例命令入口

| 命令场景 | 案例 |
| --- | --- |
| `openssl x509` / `openssl s_client` 检查证书 | `cases/server/nginx-https证书排查案例.md` |
| `ss` / `curl` / `nginx -t` 排查 502 | `cases/server/nginx反向代理502案例.md` |
| `ufw status` / `ss` 排查端口 | `cases/server/ubuntu端口不通排查案例.md` |
| `docker ps -a` / `docker logs` 查容器退出 | `cases/server/docker容器启动失败案例.md` |
| `systemctl` / `journalctl` 部署 Spring Boot | `cases/backend/springboot-systemd部署案例.md` |
| `ss` / `ps` 查端口占用 | `cases/backend/springboot端口占用案例.md` |
| `curl` / `php -m` 查 PHP 500 | `cases/backend/php接口500排查案例.md` |
| `systemctl status php-fpm` 查 PHP-FPM | `cases/backend/php-fpm异常排查案例.md` |
| `EXPLAIN` / `SHOW INDEX` 查慢 SQL | `cases/database/mysql慢SQL-explain案例.md` |
| `mysqldump` / `mysql < backup.sql` 备份恢复 | `cases/database/mysql备份恢复案例.md` |
| `SELECT COUNT` / `DELETE ... LIMIT` 清理日志表 | `cases/database/mysql日志表过大清理案例.md` |
| `DESCRIBE` / `SHOW CREATE TABLE` 查字段缺失 | `cases/database/mysql字段缺失排查案例.md` |
| `adb logcat` / `adb bugreport` 查 ANR | `cases/android/android-anr日志分析案例.md` |
| `./gradlew --stacktrace` 查构建失败 | `cases/android/android-gradle构建失败案例.md` |
| `adb shell pm list packages` 查 LSPosed 目标包 | `cases/android/lsposed模块未加载排查案例.md` |
| `adb install` 查 APK 安装失败 | `cases/android/apk安装失败排查案例.md` |
| `find` 查前端源码结构 | `cases/delivery/前端dist不是源码判断案例.md` |
| `jar tf` 查 jar 运行包结构 | `cases/delivery/jar不是后端源码判断案例.md` |
| `find` / `du` 查交付包风险文件 | `cases/delivery/客户交付包清理案例.md` |
| `find` 查三件套完整性 | `cases/delivery/前后端数据库三件套检查案例.md` |

## 检查清单入口

| 场景 | 文档 |
| --- | --- |
| 服务器上线前 | `checklists/服务器部署前检查清单.md` |
| 源码交付前 | `checklists/源码交付前检查清单.md` |
| 数据库备份恢复前 | `checklists/数据库备份恢复检查清单.md` |
| Codex 修改前 | `checklists/Codex修改前检查清单.md` |

## 第四阶段维护命令入口

| 命令场景 | 文档 |
| --- | --- |
| 空 Markdown 检查 | `workflows/知识沉淀流程.md`、`workflows/Codex协作流程.md` |
| GitHub Desktop 推送前核对 | `workflows/GitHub同步流程.md` |
| 从问题归档到 cases | `workflows/问题排查到案例归档流程.md` |
| 判断命令是否高风险 | `security/命令风险分级.md` |
| 检查敏感内容是否该写入 | `security/脱敏规则.md`、`security/禁止写入知识库的内容.md` |

## 验证方法

- 命令输出能帮助判断当前状态。
- 写操作前已经确认目标环境、路径、库名或服务名。

## 注意事项

- `restart`、`down`、`DELETE`、`DROP`、`TRUNCATE`、`rm -rf` 都属于高风险动作。
- 命令中的 `<host>`、`<user>`、`<port>` 必须替换成测试或脱敏值。

## 适用范围

知识库导航、命令检索和问题排查入口维护。

## 相关命令

`find . -type f`、`grep -R`、`git status --short`。

## 后续可补充内容

新增文档后同步补充索引入口和关键词。
